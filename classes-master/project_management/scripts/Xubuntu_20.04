#!/bin/bash
#
# Xubuntu 20.04
#
# update and install
#
sudo apt update
sudo apt -y upgrade
#
# utilities
#
sudo apt -y install vim-gtk qpdf expect xournal geany geany-plugins evince gparted hardinfo powertop htop gnome-disk-utility ntp exfat-fuse exfat-utils
#
# net
#
sudo apt -y install firefox remmina openssh-client
#
# graphics, CAD
#
sudo apt -y install geeqie gimp mypaint inkscape blender meshlab freecad
#
# office
#
sudo apt -y install libreoffice ttf-mscorefonts-installer fonts-symbola ttf-ancient-fonts
#
# project management
#
sudo apt -y install rsync git gitk git-cola
#
# media
#
sudo apt -y install ffmpeg vlc mplayer guvcview audacity kdenlive simplescreenrecorder
#
# node
#
sudo apt -y install nodejs npm
#
# code
#
sudo apt -y install build-essential libpng-dev python3-dev python3-toml libboost-all-dev libgl1-mesa-dev qt5-default make cmake freeglut3 freeglut3-dev libglew-dev libglfw3-dev libgif-dev python-is-python3
#
# EDA
#
sudo apt -y install kicad
#
# embedded programming
#
sudo apt -y install avr-libc gcc-avr binutils-avr avrdude python3-serial python3-tk
#
# math
#
sudo apt -y install python3-numpy python3-scipy python3-matplotlib
#
# TeX
#
sudo apt -y install texlive-latex-recommended texlive-fonts-recommended texlive-font-utils
